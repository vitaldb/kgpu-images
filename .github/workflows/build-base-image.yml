name: build-base-image

# Build & push the kgpu-pytorch base image to ghcr.io. Runs on:
#   - any change to kgpu-pytorch.Dockerfile / kgpu-mount-shared (auto)
#   - manual dispatch from the Actions tab
#
# Uses an arm64 runner (GB10 workers are arm64). Workflow's default
# GITHUB_TOKEN has packages:write, so the image lands at
# ghcr.io/vitaldb/kgpu-pytorch:<tag>. The repo is public, so the
# resulting package is publicly pullable by K3s without imagePullSecrets.

on:
  push:
    paths:
      - "kgpu-pytorch.Dockerfile"
      - "kgpu-mount-shared"
      - "kgpu-mount-files"
      - ".github/workflows/build-base-image.yml"
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: kgpu-pytorch.Dockerfile
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/vitaldb/kgpu-pytorch:24.10-py3
            ghcr.io/vitaldb/kgpu-pytorch:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
